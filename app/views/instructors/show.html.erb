<h1><%= @instructor.name %>'s Class</h1>
<br/>
Average student age: <%= @average_student_age %>
<br/>
<ul>
    <% @sorted_students.each do |student| %>
        <li><%= link_to(student.name, student_path(student)) %></li>
    <% end %>
</ul>